Check that all dependencies are up to date with the reference repo.

Fetch the current Cargo.toml from https://raw.githubusercontent.com/sysgrok/rs-matter-embassy/master/examples/rp/Cargo.toml and compare dependency versions with our Cargo.toml. Report any mismatches and suggest updates.
