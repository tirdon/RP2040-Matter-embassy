Build and flash the firmware to the Pico W via probe-rs.

Run `cargo run --release`. If probe-rs fails to connect, suggest checking the debug probe wiring and that probe-rs-tools is installed (`cargo install probe-rs-tools`).
