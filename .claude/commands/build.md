Build the project for RP2040 Pico W.

Run `cargo build` and report any errors. If dependencies fail to resolve, check that the git patches in Cargo.toml are reachable. If linker errors occur, verify memory.x and .cargo/config.toml are correct.
